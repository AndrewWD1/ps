import * as React from "react";
import { Link } from "gatsby";
import Layout from "../components/layout";

const NotFoundPage = () => {
  return <Layout image={0}>Error</Layout>;
};

export default NotFoundPage;
